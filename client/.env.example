# Copy this file to .env before running the client.
# Not secret - just the API's base URL - but kept out of the committed .env
# for the same consistency reason as the root .env/.env.example split.
VITE_API_URL=http://localhost:5261
VITE_HUB_URL=http://localhost:5261/hubs/changes
